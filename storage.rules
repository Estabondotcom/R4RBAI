service firebase.storage {
   match /b/{bucket}/o {
   match /users/{uid}/{allPaths=**} {
    // allow the signed-in user to read/write their own files
     allow read, write: if request.auth != null && request.auth.uid == uid;
    }
   }
 }